package __PACKAGE__

object Main {
  def greeting(name: String): String = s"Hello, ${name}!"

  def main(args: Array[String]): Unit = {
    val who = if (args.nonEmpty) args.mkString(" ") else "Bazel Scala"
    println(greeting(who))
  }
}
